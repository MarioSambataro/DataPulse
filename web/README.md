# web/

Frontend React + TypeScript + Vite con globo 3D (react-three-fiber).

Scaffold minimale in questa fase: `eslint` + `vitest` girano a vuoto.
L'app completa (Vite, globo, HUD) arriva nelle **SEZIONI 6-8**.

```bash
npm install
npm run lint
npm test
```
