# Gli script shell DEVONO avere line ending LF anche se clonati/editati su Windows:
# un CRLF rompe lo shebang/entrypoint dentro l'immagine Docker Linux.
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
